// array methods
// ? push , pop , shift , unshift, reverse

// let numList = [10, 20];
// numList.push(15, 60, 80);
// console.log(numList);
// console.log(numList);

// ? pop => removes last item from array

// let studentDetail = [`kalayan `, `pramod`, `aayush`, `ashesh`];
// studentDetail.pop();
// console.log(studentDetail);

// ? shift => removes firstitem from array
// let companies = [`tesla`, `ford`, `byd`, `mg`];
// companies.shift();
// console.log(companies);

// ? unshift => add item to start of array
// let companies = [`tesla`, `ford`, `byd`, `mg`];
// companies.unshift(`royal`, `BMW`);
// console.log(companies);

// ? reverse => changes the order of variable
// let companies = [`tesla`, `ford`, `byd`, `mg`];
// companies.reverse();
// console.log(companies);

// ? slice
// let companies = [`tesla`, `ford`, `byd`, `mg`];
// companies.slice[3];
// console.log(companies);
